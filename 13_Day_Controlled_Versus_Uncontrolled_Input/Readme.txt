Component Life Cycles
- In react each component has three main phases:
   *Mounting
   - Rendering or putting React component into the DOM is called mounting.
      - constructor() : constructor() method is excuted before any other methods, 
      - Static getDerivedStateFromProps()
      - render()
      - componentDidMount()
   *Updating
   *Unmounting